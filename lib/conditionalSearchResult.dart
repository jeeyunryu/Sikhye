import 'package:flutter/material.dart';

class ConditionalSearchResult extends StatelessWidget {
  const ConditionalSearchResult(this.productType, this.ingredients,
      {super.key});

  final productType;
  final ingredients;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
